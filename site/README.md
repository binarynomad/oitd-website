# site/

This folder is the **website root**. Everything in here is what gets deployed to hosting.

Keep working files, source material, and planning docs **outside** this folder
(in the repo root, `assets/`, or `docs/`).
